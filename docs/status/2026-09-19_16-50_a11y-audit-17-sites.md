# A11y Audit — 17-Site Fleet (axe-core on home pages)

- **Date:** 2026-09-19, ~16:45 CEST
- **Method:** puppeteer-core + axe-core 4.x, `bypassCSP`, home page only (desktop), `resultTypes: ['violations']`
- **Raw data:** session artifact `/tmp/a11y/axe-results.json` (transcribed here — /tmp is never cited)
- **Runner:** nix-store chromium 153 + axe injected per page; scope = home pages only

## Summary

| Site | Serious | Moderate | Minor | Violation kinds |
| --- | --- | --- | --- | --- |
| art-dupl | 1 | 0 | 1 | color-contrast (serious), empty-table-header |
| atomicwrite | 1 | 0 | 0 | scrollable-region-focusable |
| branded-id | 0 | 0 | 1 | empty-table-header |
| cleanwizard | 0 | 0 | 1 | empty-table-header |
| cmdguard (web.app) | 1 | 0 | 0 | scrollable-region-focusable (`pre`) |
| do-auditlog | 1 | 0 | 0 | color-contrast (`.text-bg-primary.py-3` quick-start CTA) |
| dynamicmarkdown | 0 | 0 | 1 | empty-table-header |
| emeet-pixyd | 0 | 0 | 1 | empty-table-header |
| errorfamily | 0 | 0 | 1 | empty-table-header |
| filewatcher | 0 | 0 | 1 | empty-table-header |
| go-output | 1 | 0 | 0 | scrollable-region-focusable ×3 (`.p-3` inside feature cards) |
| go-workflow-auditlog | 0 | 0 | 1 | empty-table-header |
| gogenfilter | 2 | 0 | 0 | color-contrast (badge), scrollable-region-focusable (`pre`) |
| learnings (Docusaurus) | 2 | 1 | 0 | color-contrast ×2 (DocSearch placeholder, secondary button), svg-img-alt (logo svg), heading-order (footer h3) |
| md-go-validator | 1 | 0 | 1 | color-contrast ×2 (clamp subtitle, accent CTA), empty-table-header |
| templcomponents | 0 | 1 | 1 | landmark-banner-is-top-level (header in landmark), empty-table-header |
| typespec-asyncapi | 2 | 0 | 0 | color-contrast ×2 (badge + accent CTA), scrollable-region-focusable ×2 (code panels) |

## Family-wide classes (fix once per repo, not per finding)

1. **scrollable-region-focusable (serious; 7 sites)** — scrollable `<pre>`/code
   panels and card bodies lack keyboard access. Fix: `tabindex="0"` on the
   scrollable element (and `aria-label` where it is a code sample).
   Affected: atomicwrite, cmdguard, go-output, gogenfilter, typespec-asyncapi.
2. **color-contrast on accent buttons/badges (serious; 6 sites)** —
   `.bg-accent`/`.text-bg-primary` pairs below 4.5:1 (white on violet).
   Affected: art-dupl, do-auditlog, gogenfilter, md-go-validator,
   typespec-asyncapi, learnings (Docusaurus theme + DocSearch).
3. **empty-table-header (minor; 9 sites)** — one empty `<th class="tracking-wider">`
   in a shared table component. Affected: art-dupl, branded-id, cleanwizard,
   dynamicmarkdown, emeet-pixyd, errorfamily, filewatcher,
   go-workflow-auditlog, templcomponents, md-go-validator.

## Site-specific (needs individual judgment)

- **templcomponents** — `header` nested inside another landmark; restructure
  or drop the inner `<header>` tag (keep styling).
- **learnings** — Docusaurus-inherited: DocSearch placeholder contrast,
  footer heading-order, logo svg alt. Fix via Docusaurus theme overrides in
  `src/css/custom.css` / swizzle, not by hand-editing generated HTML.
- **md-go-validator** — clamp-sized subtitle color fails contrast against its
  background (verify dark-mode variant too).

## Not-audited (out of scope this pass)

- Mobile viewports (axe ran desktop only), docs subpages, interactive states
  (focus traps in menus), and the emeet-pixyd DataStar app runtime (needs the
  daemon running; CSP rules differ under DataStar).

## Re-run 2026-09-19 ~23:45 CEST (post-fix verification)

Method identical (`/tmp/a11y/audit.js`, bypassCSP, desktop, home pages).
Result: **24 violation kinds → 4, and after a second fix round → verified
CLEAN on 13 of 17** with the residual findings below.

| Site | Before | After round 1 | After round 2 | Note |
| --- | --- | --- | --- | --- |
| art-dupl | 2 kinds | 0 | 0 | |
| atomicwrite | 1 | 1 | **0** | `.min-w-0` install `<code>` got `tabindex="0"` (round 2) |
| gogenfilter | 2 | 1 | (see note) | badge was hardcoded `#fbbf24`; now `text-amber` token; light-theme amber then measured **4.19:1** on its own tint → `--color-amber` darkened `#b45309`→`#92400e` (redeploy pending re-check) |
| typespec-asyncapi | 2 | 1 | (see note) | badge `text-accent` fails in light mode; new `--color-accent-text` token (`#0f766e` light); same amber-700→amber-800 bump; redeploy pending re-check |
| learnings | 3 | 1 | **0** | DocSearch died with the algolia placeholder removal; `.button--secondary` now white/blue via custom.css |
| all others | 0–1 | 0 | 0 | empty-th / scrollable / contrast classes verified gone |

Notes:

- The audit tool runs with headless-default **light** preference, which is
  exactly why the theme-unaware hardcoded colors failed: they had only ever
  been checked in dark mode by eye.
- The remaining two sites get a final axe pass after their redeploy lands;
  expectation is CLEAN (the failing pair measured 4.19:1, new pair ≈ 5.9:1).
- `scripts/family-a11y-guard.sh` (ADR 0001 prototype) now encodes the
  three violation classes as check-mode greps: 0 FAIL / 68 INFO across the
  family (INFO = bg-accent CTAs in repos that have not adopted the
  `--color-on-accent` token scheme — convergence backlog, not violations).
